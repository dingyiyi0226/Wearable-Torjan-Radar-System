#:kivy 1.11.1

# Trace Points
<Target>:
    canvas:
        Ellipse:
            pos: self.pos
            size: self.size
    size: 20, 20

<ControlPanel>:
    BoxLayout:

# RelativeLayout
<RadarInterface>:
    canvas:
        Color:
            rgba: 0.5, 0.5, 0.5, 1
        Rectangle:
            size: self.size
        # Line:
        #     width: 2.0
        #     ellipse: (self.parent.center_x - 0.5 * self.parent.width, self.parent.center_y - 0.5 * self.parent.height, self.parent.width, self.parent.height)
        # Line:
        #     width: 2.0
        #     ellipse: (self.parent.center_x - 0.375 * self.parent.width, self.parent.center_y - 0.375 * self.parent.height, 0.75 * self.parent.width, 0.75 * self.parent.height)
        # Line:
        #     width: 2.0
        #     ellipse: (self.parent.center_x - 0.25 * self.parent.width, self.parent.center_y - 0.25 * self.parent.height, 0.5 * self.parent.width, 0.5 * self.parent.height)

# Root: FloatLayout
<UserInterface>:
    # panel: panel
    radar: radar

    canvas:
        Color:
            rgba: 1.0, 1.0, 1.0, 1.0

        Rectangle:
            pos: self.pos
            size: self.size

    RadarInterface:
        id: radar

    # SignalOscilloscope:
    #     center: self.parent.center

    # ControlPanel:
    #     id: panel
    #     center: self.parent.center
